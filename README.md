turn "code" into an image using:
  (color([elementid])) [color] to paint an existing element
  (text([elementid])) [text] to display text
  (background) [color] to add background
it is case sensative as well as inline sensative, 
format is:
(command([elementid])) [text/color]
